# Runtime dependencies for gh-tui.
# Mirrors [project.dependencies] in pyproject.toml. Install with:  pip install -r requirements.txt
# (or the canonical way:  pip install -e .)

textual>=0.80      # TUI framework
rich>=13           # rendering (pulled in by textual)
pygit2>=1.15       # fast local git reads via libgit2
pyyaml>=6          # config loading
httpx>=0.27        # async HTTP for direct GitHub API calls
